/**
 * Configurações da aplicação - EXEMPLO
 *
 * INSTRUÇÕES:
 * 1. Copie este arquivo para config.js
 * 2. Substitua os valores com suas credenciais reais do JSONBin.io e Twilio
 * 3. NUNCA faça commit do arquivo config.js (ele está no .gitignore)
 */

const CONFIG = {
    // Configuração do JSONBin.io
    jsonbin: {
        binId: 'SEU_BIN_ID_AQUI',
        accessKey: 'SUA_ACCESS_KEY_AQUI',
        baseUrl: 'https://api.jsonbin.io/v3/b'
    },

    // Configurações de limpeza automática
    cleanup: {
        hoursEncerrado: 3,  // Horas para excluir incidente encerrado
        hoursInativo: 24    // Horas para excluir incidente sem atualização
    },

    // Limites de escalonamento
    escalonamento: {
        HFC: 10,
        GPON: 300
    },

    // Configurações de SMS (Twilio)
    // Para ativar: configure as credenciais abaixo e adicione números na interface
    sms: {
        enabled: false,  // Ativar/desativar envio automático de SMS
        provider: 'twilio',  // Provedor de SMS (twilio, vonage, etc)

        // Credenciais Twilio (obter em https://console.twilio.com)
        twilio: {
            accountSid: 'SEU_ACCOUNT_SID_AQUI',  // Ex: ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
            authToken: 'SEU_AUTH_TOKEN_AQUI',     // Ex: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
            phoneFrom: '+5511999999999'           // Número Twilio (formato: +55...)
        },

        // Números de telefone destino (gerenciados pela interface)
        recipients: [
            // Adicione números pela interface ou aqui no formato:
            // '+5511999999999',
            // '+5521888888888'
        ],

        // Template da mensagem SMS
        template: {
            maxLength: 160,        // Limite de caracteres por SMS
            prefix: '🚨 COP REDE'  // Prefixo da mensagem
        }
    }
};

// Exportar configuração
if (typeof module !== 'undefined' && module.exports) {
    module.exports = CONFIG;
}
